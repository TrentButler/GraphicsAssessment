#include "FlyCamera.h"

FlyCamera::FlyCamera() {};
FlyCamera::~FlyCamera() {};

void FlyCamera::update(float deltaTime)
{
	//update(deltaTime);
	//POLL KEYBOARD INPUT
	//POLL MOUSE MOVEMENT OVER TIME
	//MOVE CAMERA
}
